# Central Configuration for the Benchmark Framework

# --- Dataset Sizes ---
# Use space-separated values inside the quotes.
CUSTOMER_COUNTS="100000"
ORDER_COUNTS="20000000 50000000 100000000"
ORPHAN_PERCENTAGES="0.001"

# --- Path Configurations ---
# Defines the location of the SQL files used for benchmark queries.
BENCHMARK_QUERIES_PATH="sql/queries"

# --- Output File ---
# The final aggregated results will be saved here.
SUMMARY_CSV="results/benchmark_summary.csv"